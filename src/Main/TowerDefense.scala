package Main

import processing.core._


object main extends PApplet {
  
  
  
}

/*
object TowerDefense extends SimpleSwingApplication {
  def top = new MainFrame {
    title = "Tower Defense"
    val start = new Button {
      text = "Start Game"
      this.preferredSize_=(new Dimension(100,120))
      this.xLayoutAlignment_=(300)
      this.yLayoutAlignment_=(100)
    }
    val settings = new Button {
      text = "Settings"
      this.preferredSize_=(new Dimension(100,120))
      this.xLayoutAlignment_=(300)
      this.yLayoutAlignment_=(100)
    }
    val instructions = new Button {
      text = "Instructions"
      this.preferredSize_=(new Dimension(100,120))
      this.verticalAlignment_=(new Alignment(Center))
    }
    val exit = new Button {
      text = "Exit"
      this.preferredSize_=(new Dimension(100,120))
      this.xLayoutAlignment_=(300)
      this.yLayoutAlignment_=(100)
    }
    val label = new Label {
      text = "No button clicks registered"
    }	
    contents = new BoxPanel(Orientation.Vertical) {
      contents += start
      contents += settings
      contents += instructions
      contents += exit
      contents += label
      border = Swing.EmptyBorder(400, 600, 600, 400)
    }
  }
}
*/